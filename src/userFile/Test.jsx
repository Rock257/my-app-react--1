
export function Test(){
    return(
        <>
        <h1>Hello this to export multiple component through a single file </h1>
        <h2>element must wrrapped in a single element otherwise its through error </h2>
        </>
        
    )
}